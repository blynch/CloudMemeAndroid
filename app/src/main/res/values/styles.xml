<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MemeActionBar</item>
    </style>

    <style name="MemeActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/brightgreen</item>
        <item name="android:titleTextStyle">@style/MemeActionBar.TitleTextStyle</item>
    </style>

    <style name="MemeActionBar.TitleTextStyle"
        parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
